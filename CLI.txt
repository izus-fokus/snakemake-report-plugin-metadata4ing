============================================================

RUN SIMULATION

============================================================
conda create -n fenicsx-env -c conda-forge fenics-dolfinx mpich
conda activate fenicsx-env
pip install pint gmsh snakemake pyvista
snakemake --cores 1 --force


conda create -n kratos -c conda-forge KratosMultiphysics-all
============================================================

CREATE REPORTER

============================================================

conda create --name poetry python=3.12
conda activate poetry
pip install poetry

poetry self add poetry-snakemake-plugin
poetry new snakemake-report-plugin-metadata4ing
cd snakemake-report-plugin-metadata4ing

poetry scaffold-snakemake-report-plugin

============================================================

CODE

============================================================

poetry build
pip install --force-reinstall dist/snakemake_report_plugin_metadata4ing-1.2.4-py3-none-any.whl
snakemake --reporter metadata4ing --cores 1 ./paper.pdf
unzip ro-crate-metadata.zip -d ro-crate-metadata


snakemake --reporter metadata4ing --cores 1 --use-conda ./paper.pdf

snakemake --reporter metadata4ing \
    --report-metadata4ing-paramscript ../../../sample_extractor/my_extractor.py \
    --report-metadata4ing-config metadata4ing.config \
    --cores 1

snakemake --reporter metadata4ing --cores 1

snakemake --export-cwl workflow.cwl --cores 1
